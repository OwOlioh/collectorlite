# 2026-08-27

- 修复应用启动时因本地数据库已应用缺失的 migration 7（`multi source items`）而 panic 闪退的问题。将 `src-tauri/src/state.rs` 的数据库文件名从 `bili_collector.sqlite3` 切换为 `bili_collector_v2.sqlite3`，使当前源码按现有 0001–0006 迁移创建干净数据库；旧库未删除或修改。
- 验证结果：`cargo fmt -- --check`、`cargo check`、`cargo build`、`npm.cmd run build` 均通过；修复后的桌面程序持续运行 5 秒未退出，原 migration 7 panic 不再出现。
- 建立 Git 版本管理：添加 .workbuddy 到 .gitignore，提交稳定版本，打 v1.0.0 tag。
- 实现浏览器书签导入功能（feat/browser-bookmarks 分支）：
  - 后端新增 `source/browser.rs`，解析 Netscape Bookmark HTML 格式，实现 `SourceAdapter` trait
  - 新增 `import_browser_bookmarks` Tauri 命令，支持将 HTML 文件内容直接导入
  - 书签的 `source` 设为 "browser"，`external_id` 基于 URL 的 SHA256 hash
  - 自动解析文件夹层级路径作为 description 和 partition_name
  - 前端 ImportPage 新增"浏览器书签"Tab，支持拖拽/选择 HTML 文件，预览和导入
  - LibraryPage 卡片适配 browser 类型：隐藏 UP主/时长，显示 Globe 图标，展示文件夹路径
  - 3 个单元测试通过（解析、跳过 JS 链接、嵌套文件夹）
  - cargo check + cargo test + npm build 全部通过

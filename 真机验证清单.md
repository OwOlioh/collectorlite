# collectorlite 真机联调验证清单

> 这份清单覆盖所有「代码已提交、但从未在真机跑过」的原生集成。每项打勾 = 真机通过。
> 跑出任何不对劲的地方，把**现象 + 复现步骤**贴给我，我定位修。
> 底层事实来自 `capture.rs` / `nowplaying.rs` / `source/netease.rs` / `uri.rs` / `open_prefs.rs` / `commands.rs` 及注释，未改动代码。

## 0. 前置准备（先确认环境对）

- [ ] **必须用 `cargo run` 启动桌面端**，不要只跑 `npm run dev` 单开前端——纯 webview 无法唤起系统协议(`orpheus://`)、建原生窗口、读窗口标题。
- [ ] `cargo build` + `npm run build` 都通过，启动无 Rust panic。
- [ ] 网易云客户端已登录且在播放；Edge 已加载 `extension/`；要测 Obsidian 联动时 Vault 已配置。

---

## 1. 浏览器侧边栏插件（Edge extension + Rust 桥）

**涉及**：`extension/`(manifest/sidepanel/options/sw) + `capture.rs`(`tiny_http` 桥，`/capture` `/note`，token 认证) + `commands.rs`(`get_bridge_info`/`regenerate_bridge_token`)

### 1.1 安装与连接
- [ ] 扩展装到 Edge，点工具栏图标展开侧边栏（`openPanelOnActionClick`）。
- [ ] 设置页复制 bridge token → 粘贴进扩展选项页（`options.js` 存入 `bridgeToken`，并把 `bridgePort` 置 `null`）。
- [ ] 侧边栏显示「已连接」：扩展按上次端口 + 顺序扫 `17820–17829` 找到桥。
- [ ] ⚠️ **已知风险**：10 个端口全被占 → 桥起不来但 app 照常跑，侧栏报「连不上 collectorlite，请先启动应用」（`capture::start` 失败仅 `eprintln`）。此时重启 app 换端口应能连。
- [ ] ⚠️ **已知风险**：token 错 / 桥没起 → 侧栏报连不上（扩展读不到 app 数据目录，token 必须手动粘贴一次）。

### 1.2 收藏视图
- [ ] 打开 B站/知乎/CSDN/GitHub 页面 → 侧栏自动识别当前页（注入脚本读 `og:title/og:image`，失败回退 `tab.title`），填标题/标签 → 提交 → 弹「已保存」，应用库里出现该条。
- [ ] 已存在条目再提交 → 标题/标签更新，但**不**清空封面 / `extra_json` / 书签导入的 `folder_tags`（收藏视图 `note` 为 `None` 时原样保留）。
- [ ] 浏览器内部页（`chrome://` / `edge://` / `about:`）→ 友好提示「不可收藏」，不报错。
- [ ] ⚠️ **已知风险**：B站 `?p=N` 是第几集，**归一化绝不剥 `p`**，剥了会把两集认成同一条；不同入口 `spm` 变体下仍按 BV 号命中。

### 1.3 笔记视图
- [ ] 先进「收藏」收进来，再切「笔记」→ 能编辑 notes；未收藏页进笔记视图被锁（`nb-locked`）。
- [ ] 笔记乐观锁：在应用里改了同一条 notes，侧栏再保存 → 弹冲突框（`cf-overwrite` 以服务端 `remoteUpdatedAt` 为新基准 / `cf-reload` 拉回远程）。
- [ ] 笔记保存后 Obsidian 联动自动生效（`/obsidian/open` 走 Rust 侧 `ShellExecute`，不会被浏览器拦截 `obsidian://`）。
- [ ] ⚠️ **已知风险**：`/note` 对未收藏页返回 404「这一页还没收藏，先在『收藏』里收进来」；草稿兜底仅在「服务端为空且有本地草稿」时恢复。

### 1.4 其它
- [ ] `/ping`(探活) / `/tags`(标签池) / `/item?url=`(已收藏查询) 正常。
- [ ] 设置页「重新生成 token」→ 旧 token 立刻失效，侧栏需重新粘贴。
- [ ] ⚠️ **已知风险**：`MAX_BODY_BYTES=64KB` 超界截断；CORS Origin 故意放开（token 才是防线）。

---

## 2. 网易云速记浮窗（nowplaying）

**涉及**：`nowplaying.rs`(Ctrl+Alt+S / 按需 destroy / `OrpheusBrowserHost` 筛 / `SetWinEventHook` 吸附) + `src/nowplaying/`(手动时间轴) + `SettingsPage`(开关/兜底按钮)

- [ ] 设置页开启「速记浮窗」，`Ctrl+Alt+S` 唤起面板（置顶 / 无装饰 / 不进任务栏）。
- [ ] ⚠️ **已知风险**：快捷键被占用时**无任何 UI 告警**（仅启动日志 `eprintln`），但设置页「打开面板」兜底按钮仍能开。快捷键改动需重启 app 生效。
- [ ] 网易云正在播歌 → 面板自动读到「曲名 - 歌手」；迷你模式 / 未播放 → 给**准确提示**（如「迷你播放器模式」）而非笼统「未在播放」（`NO_TRACK_TITLES`/`IDLE_TITLES` 区分）。
- [ ] ⚠️ **已知风险**：必须按窗口类名 `OrpheusBrowserHost` 筛，同属 `cloudmusic.exe` 的 `MiniPlayer`/`DesktopLyrics` 等带标题但不是曲目来源，筛错会认错歌。
- [ ] 面板吸附网易云主窗口：移动/最小化/关闭网易云 → 面板跟随/关闭；网易云没开 → 落到屏幕右缘（`place_at_edge`，注意高 DPI 飞屏）。
- [ ] 手动时间轴：播放/暂停/重置/滑块**全听用户**，不依赖网易云信号；切歌（标题变化）自动归零 + 停表。
- [ ] ⚠️ **已知风险**：WASAPI 播放检测已标 `dead_code`——网易云暂停时音频会话仍 Active，且会话在非默认渲染设备，故计时改手动时间轴，前端不读真实信号。
- [ ] 匹配不到的歌 → 用合成 id `np:{曲名}|{歌手}` 落库（`unresolved:true`），同一首反复记不堆重复。
- [ ] 打标签 / 写批注 → 落库 + Obsidian 联动（`notes::save_notes`）。
- [ ] 关闭面板是 **destroy 不是 hide**（再开是全新窗口，不常驻/不轮询）。

---

## 3. 网易云深链打开 + 打开方式偏好

**涉及**：`source/netease.rs`(`song_play_uri`/`open_song_in_client`) + `uri.rs`(`open_uri_system`，查注册表) + `open_prefs.rs` + `VideoCard`(hover 双出口)

- [ ] 网易云条目点封面/标题 → 唤起客户端并播放（`orpheus://` + base64 `{"type":"song","id":...,"cmd":"play"}`）。
- [ ] ⚠️ **已知风险**：唯一实测可用格式就是上面这个；`orpheus://song/{id}`、`orpheus://openurl?url=` 都无反应。`cmd:"play"` 会**替换当前播放的歌**（已拍板接受）。
- [ ] 客户端未装 / 协议未注册 → 自动回退浏览器 + toast「未检测到网易云客户端，已在浏览器打开」。
- [ ] ⚠️ **已知风险**：`ShellExecute` 返回码不可信（不存在的协议也返回 42），故**必须先查注册表预检**再决定回退，否则兜底永不触发、点了完全没反应。
- [ ] 设置页「打开方式」切 客户端优先 / 浏览器 → 卡片行为随之变；hover 菜单**始终**提供另一方向出口（客户端优先时显示「浏览器」按钮，反之显示「客户端」按钮）。Rust 侧兜底读 `open_prefs`，所有路径（卡片/回收站/批量）都尊重偏好。
- [ ] `npm run dev` 单开前端点客户端 → 提示「浏览器预览模式无法唤起客户端，请用 `cargo run` 启动桌面端」（mock 恒返回 false，不误导）。
- [ ] ⚠️ **已知风险**：后端没重编译就点 → 捕获 `command ... not found` 提示重编译（本项目无 HMR）。

---

## 4. 网易云歌单增量同步

**涉及**：`source/netease.rs`(`plan_incremental`/`pick_unfavorited`/`SyncSettings`) + `commands.rs`(`sync_netease`/`start_netease_sync_loop`，启动延迟 20s) + `NeteaseSyncListener` + `SettingsPage`

- [ ] 导入歌单 → 自动登记进同步范围（立水位 = `max(favorite_time)`）。
- [ ] 启动 `NETEASE_STARTUP_DELAY_SECS=20` 秒后跑第一轮；之后每 `interval_minutes`（默认 15，下限 5）轮询；关开关时循环空转不退出（免重启）。
- [ ] 新收藏的歌 → 下一轮增量入库（不重扫全量）；水位前推取全表最大值防倒退。
- [ ] 取消收藏 → 软删进回收站（开关默认开）。
- [ ] ⚠️ **三道安全阀，务必逐一验**：
  1. 导入时立水位（不传则首轮前新收藏的歌会漏）。
  2. **任一歌单拉失败 → 本轮跳过取消收藏清理**（否则会把整张歌单误判成「用户全删了」）；歌单突然 0 首也按接口异常跳过。
  3. **按所有同步歌单的并集判定**：同一首跨 A、B 两歌单，只在并集都不在才删。
- [ ] 设置页「立即同步」→ 忽略间隔强制跑，但**仍**需登录态 + 已登记歌单（强行跑只白挨风控）。
- [ ] ⚠️ **已知风险**：同步间隔低于 5 分钟会被钳到 5（`MIN_SYNC_INTERVAL_MINUTES=5`），否则触发网易云风控 `8821/-462`。
- [ ] ⚠️ **已知风险**：`fetch_songs` 一次传 >201 个 id 会**静默截断**成 201，分块固定 200；`trackIds` 必须严格倒序，否则漏歌。
- [ ] toast 仅在有变化（`added>0 || removed>0`）时弹；纯空轮不弹（避免每 15 分钟打扰）；`errors` 仍展示。

---

## 5. 启动链路 + 封面后台缓存（断点续传）

**涉及**：`lib.rs` setup(`capture::start` / `cover_cache::spawn_cover_cache` / `start_netease_sync_loop` / 全局快捷键) + `App.tsx`(`autoPurgeTrash`) + `cover_cache.rs`

- [ ] `cargo run` 启动：桥起来、同步循环注册、快捷键注册——任一失败**仅告警不阻断**（端口占满 / 快捷键被占场景）。
- [ ] 封面缓存：导入后关应用再开 → 未缓存封面（`cover_url` 有值但 `cover_local_path` 为空）自动补完；不重复下载、不丢任务（数据库即队列，文件名由 `source+external_id` 决定）。
- [ ] ⚠️ **已知风险**：封面续传最坏只浪费已下载流量、不损坏数据；扩展名变化可能留极少量孤立文件（代价可忽略）。
- [ ] ⚠️ **已知风险**：自动清回收站由**前端 `App.tsx` 启动触发**（非 Rust setup），确认前端窗口已起来才会跑；超期项硬删 + 清封面（限 `covers/` 目录）。

---

## 报告模板（跑出问题贴这个给我）

```
【功能块】1 / 2 / 3 / 4 / 5
【现象】实际发生了什么
【预期】按清单应该发生什么
【复现步骤】1. 2. 3.
【环境】网易云版本 / Edge 版本 / Windows 版本 / 是否 cargo run
【日志】相关 Rust eprintln 或前端 console（如有）
```
